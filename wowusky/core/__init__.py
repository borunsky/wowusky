"""Core utilities for wowusky.

The ``core`` package holds everything that doesn't depend on a GUI or
on a specific addon source. The rest of the application (providers,
GUI tabs, CLI) builds on top of these primitives.
"""
